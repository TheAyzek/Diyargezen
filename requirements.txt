# Modern FRP Karakter Oluşturucu - Bağımlılıklar

# Core dependencies
rich>=13.0.0
inquirerpy>=0.3.0
pyyaml>=6.0
pydantic>=2.0.0

# GUI dependencies (mevcut sistem için)
PySide6>=6.5.0
qdarkstyle>=3.0.0

# PDF export
reportlab>=4.0.0

# Image processing (logo için)
Pillow>=10.0.0

# PDF parsing (kural kitabı için)
PyPDF2>=3.0.0

# NLP for advanced rule extraction (opsiyonel)
# spaCy>=3.7.0
# python -m spacy download en_core_web_sm

# Development dependencies
pytest>=7.0.0
black>=23.0.0
flake8>=6.0.0

# Build dependencies
pyinstaller>=6.0.0